[package]
name = "alp-toolkit"
version = "0.1.1"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
log = []

[dependencies]
alptk-log = { version = "0.1.0", path = "../log" }
owo-colors = "4.0.0"
